experiment_id: "feat_exp_combined"
description: "Campaign B: Feature Engineering Experimental Track — Baseline + Local Variance + SAVI + MSAVI (24 features, Experimental Approximation)"
campaign_type: "feature_engineering"
track: "experimental"
model:
  model_id: "rf_enhanced_v1"
  hyperparameters:
    n_estimators: 100
    random_state: 42
    n_jobs: -1
dataset:
  dataset_id: "dholera_sentinel_v1"
  test_size: 0.20
  random_state: 42
features:
  local_variance:
    enabled: true
    window_size: 3
  savi:
    enabled: true
  msavi:
    enabled: true
baseline_lock:
  inherit_preprocessing: true
  inherit_feature_engineering: false
  deviations: ["Local Variance + SAVI + MSAVI (Experimental Approximations) added"]
ablation: {}
