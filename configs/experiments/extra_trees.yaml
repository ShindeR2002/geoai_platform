experiment_id: "exp_extra_trees"
description: "ExtraTrees classifier baseline"
model:
  model_id: "extra_trees"
  hyperparameters:
    n_estimators: 100
    random_state: 42
    n_jobs: -1
dataset:
  dataset_id: "dholera_sentinel_v1"
  test_size: 0.20
  random_state: 42
baseline_lock:
  inherit_preprocessing: true
  inherit_feature_engineering: true
  deviations:
    - "Alternative model family: ExtraTrees classifier baseline"
ablation: {}
